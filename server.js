// Next.js server entry point
// Database initialization happens in _app.js or via a startup script
require('dotenv').config();

// This file is kept for compatibility but Next.js handles the server
// Run: npm run dev or npm start

if (require.main === module) {
  console.log('Starting Next.js server...');
  console.log('Run: npm run dev (development) or npm start (production)');
}
